From 390eec2129c7dbcf2c1e0f2e516c39fa7a2f16d2 Mon Sep 17 00:00:00 2001
From: Xiaolei Wang <xiaolei.wang@windriver.com>
Date: Tue, 26 Nov 2024 16:38:14 +0800
Subject: [PATCH] mxm_wifiex: Delete REGULATORY_IGNORE_STALE_KICKOFF

This configuration has been deleted by kernel commit e8c2af660ba0,
so remove it

Upstream-Status: Pending

Signed-off-by: Xiaolei Wang <xiaolei.wang@windriver.com>
---
 mlinux/moal_sta_cfg80211.c | 21 ---------------------
 1 file changed, 21 deletions(-)

diff --git a/mlinux/moal_sta_cfg80211.c b/mlinux/moal_sta_cfg80211.c
index 3a487b4..ae4e536 100644
--- a/mlinux/moal_sta_cfg80211.c
+++ b/mlinux/moal_sta_cfg80211.c
@@ -10594,27 +10594,6 @@ mlan_status woal_register_cfg80211(moal_private *priv)
 	} else {
 		PRINTM(MIOCTL, "Follow countryIE provided by AP.\n");
 	}
-#endif
-#if CFG80211_VERSION_CODE >= KERNEL_VERSION(3, 19, 0)
-#if CFG80211_VERSION_CODE <= KERNEL_VERSION(6, 1, 38)
-	/*REGULATORY_IGNORE_STALE_KICKOFF: the regulatory core will _not_ make
-	 * sure all interfaces on this wiphy reside on allowed channels. If this
-	 * flag is not set, upon a regdomain change, the interfaces are given a
-	 * grace period (currently 60 seconds) to disconnect or move to an
-	 * allowed channel.*/
-	wiphy->regulatory_flags |= REGULATORY_IGNORE_STALE_KICKOFF;
-#endif
-#endif
-
-#if CFG80211_VERSION_CODE >= KERNEL_VERSION(6, 2, 0)
-#if CFG80211_VERSION_CODE <= KERNEL_VERSION(6, 3, 12)
-	/*REGULATORY_IGNORE_STALE_KICKOFF: the regulatory core will _not_ make
-	 * sure all interfaces on this wiphy reside on allowed channels. If this
-	 * flag is not set, upon a regdomain change, the interfaces are given a
-	 * grace period (currently 60 seconds) to disconnect or move to an
-	 * allowed channel.*/
-	wiphy->regulatory_flags |= REGULATORY_IGNORE_STALE_KICKOFF;
-#endif
 #endif
 	memset(&priv->phandle->country_code, 0,
 	       sizeof(priv->phandle->country_code));
-- 
2.25.1

